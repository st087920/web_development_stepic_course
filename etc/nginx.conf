server {
    listen 80;
    location /uploads/ {
        root /home/box/web;
    }
    location ~ \.(ico|jpeg|jpg|png|css|js)$ {
        root /home/box/web/public;
    }
    location / {
        return 404;
    }
}
